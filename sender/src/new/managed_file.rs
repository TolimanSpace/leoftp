/// Managed file structure:
///
/// Marker mode:
/// - `header.json` contains the header in a human readable format
/// - `header.bin` contains the header in a binary format. This is what's read by the algorithm.
/// - `data.bin` contains the full file data in a binary format
/// - `markers`
pub struct ManagedFile {

}